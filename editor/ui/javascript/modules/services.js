(function(){

	var app = angular.module('mlrepl.services', []);

	// app.service('ResourceService', function($http){
	// 	this.getValues = function(resource, val){
			
	// 		return $http.get('/dashboard/api/'+resource, {
	// 	        params : {
	// 	          q : val
	// 	        }
	// 	      }).then(function(resp){
		        
	// 	        return (resp.data) ? (resp.data instanceof Array) ? resp.data : [resp.data] : '';

	// 	      });
		
	// 	};
	// });

})();